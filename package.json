{
    "name": "jsontotype",
    "displayName": "jsonToType",
    "description": "convert json file into type interface",
    "version": "0.0.1",
    "publisher": "danielDing",
    "engines": {
        "vscode": "^1.0.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:extension.convertJsonToType"
    ],
    "main": "./out/src/extension",
    "contributes": {
        "commands": [{
            "command": "extension.convertJsonToType",
            "title": "json to type"
        }]
    },
    "scripts": {
        "vscode:prepublish": "node ./node_modules/vscode/bin/compile",
        "compile": "node ./node_modules/vscode/bin/compile -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install"
    },
    "devDependencies": {
        "typescript": "^1.8.5",
        "vscode": "^0.11.0"
    }
}